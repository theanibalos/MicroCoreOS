# Un simple objeto de datos (DTO) para estandarizar el paso de información
class UserModel:
    def __init__(self, id, name, email):
        self.id = id
        self.name = name
        self.email = email

    def to_dict(self):
        return {"id": self.id, "name": self.name, "email": self.email}